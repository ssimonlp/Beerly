<style type="text/css">
  #map {
    width: 100%;
    height: 100%;
    border-radius: 3px;
  }

</style>

<div class="container pt-4">

  <div class="row">
    <div class="col-sm-3 col-md-3 col-lg-3">
      <% @bars.each do |bar| %>
      <div class="card mb-2">
        <%= image_tag("lager.jpg", class: "card-img-top", alt: "beer") %>
        <div class="card-body">
          <h5>
            <%= bar.name %>
          </h5>
          <p>
            <%= bar.address %>
          </p>
          <%= link_to "Voir", bar_path(bar.id), class:"text-warning" %>
        </div>
      </div>
      <% end %>
    </div>
    <div class="col-sm-9 col-md-9 col-lg-9">
      <div id="map"></div>
    </div>
  </div>
</div>

<script src="https://maps.googleapis.com/maps/api/js?key=<%= Rails.application.credentials.google_maps %>&callback=initMap" type="text/javascript"></script>
<script src="/javascript/lib/gmaps.js"></script>
<script src="https://code.jquery.com/jquery-3.3.1.min.js" integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8=" crossorigin="anonymous"></script>
<script>
  $.getJSON("/json/style.json", function(json) {
    newMap(json);
  });

  function newMap(data) {
    var map = new GMaps({
      div: '#map',
      disableDefaultUI: true,
      styles: data
    });
    var latLong = [];
    <% @bars.each do |bar| %>
    map.addMarker({
      lat: <%= bar.latitude %>,
      lng: <%= bar.longitude %>,
      title: 'Lima',
      icon: '/img/beer32.png',
      infoWindow: {
        content: "<%= bar.name %>"
      }
    });
    latLong.push({lat: <%= bar.latitude %>,
      lng: <%= bar.longitude %>});
    <% end %>
    map.fitLatLngBounds(latLong);
  };

</script>
